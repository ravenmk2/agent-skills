# Agent Skills Repository

本仓库为个人常用 Agent Skills 的合集。收录了我为 AI 编程助手编写和沉淀的各种技能，涵盖日常工作流中的实用场景，并随使用情况持续补充和打磨。

## 技能列表

### Meta Skills

| 技能          | 说明                                                   |
| ------------- | ------------------------------------------------------ |
| skill-engineer | 编写与审查技能的最佳实践与红线（含脚本工程规范）      |

## 写作要求

- 使用业内专业、严谨的术语，不使用自造词汇和概念。
- 使用结构化的排版，不使用冗长的文字段落。

### 技能描述写法

description 是触发器而非文档，读者是做匹配决策的 Agent：

- 用用户的语言（用户发起任务时的原话，如 `create, edit, optimize a skill`），不用抽象概括（如 `working on`）——抽象对人友好，对匹配致命。
- 触发范围覆盖「决策点之前」（如 `may contain executable scripts`），但不超过技能内容能服务的边界，避免错载。
- 避免隐含阶段限定的词（如 `BEFORE`），Agent 会认为任务已开始就不适用。
- 祈使语气（`MUST use`）作用有限：先被选中，强制力才有意义，语义对齐优先于语气强度。

## 工作规范

- Git 提交遵循 Git Conventional Commits 规范, 提交时先拟定提交消息、用工具向用户询问确认后再执行。
